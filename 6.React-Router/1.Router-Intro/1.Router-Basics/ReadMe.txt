Setup to implement the React Router dependency

After we create the name of the project and use cd project-name we will need to run this command:


    npm i react-router-dom localforage match-sorter sort-by

And then we will be able to access the router library and can start our server
